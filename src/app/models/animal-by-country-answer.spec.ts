import { AnimalByCountryAnswer } from './animal-by-country-answer';

describe('AnimalByCountryAnswer', () => {
  it('should create an instance', () => {
    expect(new AnimalByCountryAnswer()).toBeTruthy();
  });
});
