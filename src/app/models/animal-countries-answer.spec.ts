import { AnimalCountriesAnswer } from './animal-countries-answer';

describe('AnimalCountriesAnswer', () => {
  it('should create an instance', () => {
    expect(new AnimalCountriesAnswer()).toBeTruthy();
  });
});
